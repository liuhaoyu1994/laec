  <%= form_for(publication.publish_user_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :user_id, user.id %></div>
    <div><%= hidden_field_tag :publication_id, publication.id %></div>
    <%= f.submit "Add to author", class: "btn btn-primary" %>
  <% end %>