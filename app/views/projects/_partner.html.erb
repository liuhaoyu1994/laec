  <%= form_for(project.partner_relations.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :project_id, project.id %></div>
    <div><%= hidden_field_tag :research_partner_id, research_partner.id %></div>
    <%= f.submit "Add to author", class: "btn btn-primary" %>
  <% end %>