  <%= form_for(project.publish_relationships.build, remote: true) do |f| %>
    <div><%= hidden_field_tag :project_id, project.id %></div>
    <div><%= hidden_field_tag :publication_id, publication.id %></div>
    <%= f.submit "Relate", class: "btn btn-primary" %>
  <% end %>